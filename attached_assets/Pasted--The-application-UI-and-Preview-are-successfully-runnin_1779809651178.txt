"The application UI and Preview are successfully running, but form submissions (Sign In / Register) are failing to execute or save. This is due to an unconfigured or pending database sync in this new container.

Please resolve this authentication and connectivity issue step-by-step:

1. Database Connection & Environment Check:
Verify that the `DATABASE_URL` secret is being correctly pulled into the project's environment configuration.

2. Schema Push & Synchronization:
Run the correct Drizzle migration schema push (e.g., `npx drizzle-kit push` or the corresponding drizzle sync command) to ensure our tables inside our schema configuration are strictly mapped and aligned with the PostgreSQL instance without wiping any existing user data.

3. API Route Authentication Fix:
Check our Next.js/Expo API endpoint for signup/signin logic (e.g., `/api/auth/register` or similar authentication backend handlers). Ensure that errors from the Drizzle database client are properly logged in the terminal so we can trace any validation issues or missing fields.

Once the schema is pushed and database queries are executing properly, confirm so we can test the account login again."


"We are upgrading the core infrastructure of our 'Nexus / Oasis - Social Business Super App' to enterprise-grade decentralized standards. We need to implement three advanced architectural pillars to optimize network resilience, payment reliability, and document integrity. 

Please carefully implement the following infrastructure updates step-by-step across our Next.js/Expo and Drizzle ORM codebase without introducing breaking regressions to the current UI:

PART 1: Smart RPC Cache & Failover Router
1. Create a dedicated JSON-RPC Backend Proxy/Middleware for handling Pi Testnet API interactions (`rpc.testnet.minepi.com`).
2. Implement a local memory micro-caching layer for read-only blockchain queries (e.g., retrieving app directory configurations or total Pi committed fields). Cache hits should resolve within milliseconds without straining the external RPC.
3. Establish a soft fallback mechanism: if the primary Pi RPC latency spikes or returns a 5xx error, gracefully handle the exception so the UI doesn't crash or freeze indefinitely.

PART 2: Idempotent Escrow Engine & Webhook Queue
1. Refactor the financial transaction endpoints (`/api/escrow`, `/api/transactions`) to natively enforce strict Idempotency using an `Idempotency-Key` (UUID) generated by the frontend.
2. Ensure that duplicate clicks or accidental form multi-submissions on 'Invest' or 'Interest Expressed' triggers do not process duplicate writes to the `SmartAgreement` database table.
3. Enforce safe transactional sequencing: separate the execution of on-chain Pi ledger actions from the Drizzle database updates via an event-driven logic check, ensuring that database model states strictly match actual block transactions.

PART 3: Decentralized Document Integrity (IPFS Anchor)
1. Update the `ProjectDocument` and `SmartAgreement` upload handlers to support decentralized storage anchoring.
2. Integrate a mockable helper utility structured for IPFS pinning services (e.g., Pinata). When a project document or legal contract is signed, generate its cryptographic CID (Content Identifier/Hash).
3. Store this immutable CID directly inside the Drizzle `termsHash` or document record, guaranteeing that no baseline project data or contract rules can be modified server-side without failing cryptographic verification.

Please execute these infrastructure modules cleanly, synchronize the Drizzle schema configurations if necessary, and ensure the dev preview remains fully operational and stable."


"Now that the backend database schema and infrastructure are fully synchronized, we need to design and implement the user-facing UI flows for project classifications, legal documents, and smart contract tracking, as nothing is currently visible in the preview.

Please implement the following visual layout updates and UI components cleanly:

PART 1: Project Details Verification Tabs
1. Inside the main Project view, add a dedicated, prominent 'Verification & Trust' tab/section.
2. Render absolute visual status badges based on the project's execution state enum: Show a gray badge for 'IDEA' and a verified green badge for 'IN_PROGRESS'.
3. Create a clean UI list displaying the required legal documents submitted by the project owner. Beside each document, display its current verification flag (e.g., 'PENDING' or 'APPROVED') based on the server-side data.

PART 2: Financial Escrow Interception Modal
1. When a user clicks the 'Invest' or 'Interest Expressed' action buttons, intercept the click with an interactive Transaction Modal instead of an instant background execution.
2. Inside this Modal, visually present the digital agreement rules to the user, including the targeted `projectId`, the amount of `totalPiCommitted`, and a shortened string representation of the cryptographic `termsHash`.
3. Provide visual step-by-step loading states (e.g., 'Initiating Pi Wallet Connection...', 'Locking Funds in Escrow...', 'Agreement Active!') to reflect the real-time status transitions (`DRAFT` -> `LOCKED_IN_ESCROW` -> `ACTIVE`).

PART 3: Minimal Admin Verification Panel
1. Build a simple, restricted dashboard view or administrative section where a trusted validator can review pending project document flows.
2. Add interactive button toggles allowing the validator to flag submitted legal documents as APPROVED. This trigger must instantly update the project classification state from 'IDEA' to 'IN_PROGRESS' in the backend.

Ensure all layout bounds are padded correctly to avoid text occlusion, and that the UI seamlessly connects to our existing Drizzle state handlers."


"We need to specifically optimize the UI and application flow for 'Talent/Idea Creators' who possess the skills and technical know-how but have zero initial funding. The infrastructure must cleanly support this milestone-driven crowdfunding logic.

Please implement the following interface updates:

1. Skill & Proof-of-Capability Showcase:
Inside the Project Creation wizard and details view, rename or update the document section so that independent creators with zero capital can upload non-corporate proof—such as a personal Portfolio, technical project blueprints, or a video pitch instead of commercial registration documents.

2. Milestone-Based Escrow Release UI:
Design a step-by-step visual roadmap for the project funds. Show clearly that when a user backs an 'IDEA', their Pi is locked safely in the `SmartAgreement` (`LOCKED_IN_ESCROW`). 
Render a 'Submit Milestone Proof' button visible ONLY to the project creator, allowing them to upload images or text links as `Proof of Work` for Phase 1.

3. Backer Approval & Release Action:
Create a matching visual checklist for the campaign backers or validators. Once they inspect the uploaded proof of work, they can vote or click 'Verify Milestone' which triggers the release of that specific milestone's fractional payout directly to the creator's wallet.

Ensure this flow feels seamless, clear, and highly encouraging for grassroots creators starting with a zero budget."


"We are ready to enhance the user experience and interface organization for our 'Oasis / Nexus - Social Business Super App' based on the core application context. 

Please implement the following three layout updates and functional integrations cleanly into our frontend and database schema:

1. Real-time Pi Wallet Header Status:
- Integrate a dynamic wallet status widget inside the application's core navigation header.
- Render the authenticated user's shortened Pi Wallet address (e.g., `GAA3...7X2P`) alongside their real-time Pi token balance.
- Ensure this balance layout stays visually synchronized and updates dynamically whenever an escrow transaction or milestone action triggers on the platform.

2. Creator Milestone & Proof Manager Dashboard:
- Build a dedicated dashboard area under 'My Active Campaigns' tailored for project creators who start with zero initial funding.
- Organize this view using structured cards for each milestone phase. Each card must visibly render the phase description, the targeted deadline, and the fraction of Pi locked in the `SmartAgreement` for that specific milestone.
- Provide a modern file upload interface (Drag & Drop or attachment helper) inside each milestone card allowing creators to seamlessly submit their 'Proof of Work' (reports, blueprints, or portfolio images) to their backing Circle.

3. Dynamic Trust & Experience Score Component:
- Implement a responsive 'Trust & Knowledge Score' badge or verified level indicator adjacent to the project owner's profile handle.
- Wire this indicator to dynamically increase based on successful backend triggers, such as when a creator successfully completes a milestone, or when a backing Circle votes to verify their proof of capability.

Please execute these workspace updates step-by-step, ensuring absolute padding bounds across the React Native/Expo and Next.js views so that components do not overlap, and verify that the local dev preview environment remains fully stable and integrated with the active Groq AI module setup."